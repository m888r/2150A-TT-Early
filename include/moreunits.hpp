#pragma once

#include "units.h"

// namespace units {
//   namespace angular_acceleration {
//     //using revolutions_per_minute2 = units::compound_unit<angular_velocity::revolutions_per_minute, inverse<time::minute>>;
//   };

//   namespace angular_jerk {
//     //using revolutions_per_minute3 = units::compound_unit<angular_acceleration::revolutions_per_minute2, inverse<time::minute>>;
//   }

//   UNIT_ADD(angular_acceleration, revolutions_per_minute2, revolutions_per_minute2, rpm2, units::compound_unit<angular_velocity::revolutions_per_minute, units::inverse<time::minute>>);
//   UNIT_ADD(angular_jerk, revolutions_per_minute3, revolutions_per_minute3, rpm3, units::compound_unit<angular_acceleration::revolutions_per_minute2, units::inverse<time::minute>>);

// };
